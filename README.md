# my-glass

## Requirements

- Node.js 20.x (newer versions may fail to build native dependencies)
- Python 3
- On Windows: Build Tools for Visual Studio

## Setup

First-time install, build and run:

```bash
npm run setup
```

## Run

Once set up:

```bash
npm start
```
